# Qiskit 2.x Practice Exam II

1. Which Pauli operator can a user expect when running the following circuit?

  ```python
  from qiskit import QuantumCircuit
  from qiskit.quantum_info import Pauli
  
  qc = QuantumCircuit(1)
  qc.x(0)
  qc.y(0)
  qc.z(0)
  ```
A. 
```python
Pauli("I")
```
B. 
```python
Pauli("-iI")   
```
C. 
```python
Pauli("Y")  
```
D. 
```python
Pauli("H")  
```

2. Which is the resulting SparsePauliOp?
```python
SparsePauliOp.from_sparse_list([("X",(4,),6)],num_qubits=10)
```
A. 
```python
SparsePauliOp(['IIIIIXIIII'],
              coeffs=[6.+0.j])
```

B. 
```python
SparsePauliOp(['IIIIXIIIII'],
              coeffs=[4.+0.j])
```

C.
```python
SparsePauliOp(['IXIIII'],
              coeffs=[10.+0.j])
```

D. 
```python
SparsePauliOp(['IIIIIX'],
              coeffs=[4.+0.j])
```

3. Given the following code fragment what is the probability that a measurement would result in a value of 0?

```python
from qiskit import QuantumCircuit
from qiskit.quantum_info import Statevector
import numpy as np

qc = QuantumCircuit(1)
qc.x(0)
qc.y(0)
```

A. 0   
B. 0.146  
C. 0.5  
D. 0.853  
E. 1  

4. Which is the data which generates the following histogram?
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/Q4.png>

A.
```python
data = [{'00':40,'11':60,'01':30,'10':70}]
```

B. 
```python
data = [{'00':40,'11':60},{'01':30,'10':70}]
```

C.
```python
data = [{'00':40,'01':60},{'10':30,'11':70}]
```

D.
```python
data = [{'00':40},{'11':60},{'01':30},{'10':70}]
```

5. Which is the fragment of code which generates the following state Hinton diagram?
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/Q5.png>

A. 
```python
from qiskit import QuantumCircuit
from qiskit.visualization import plot_state_hinton

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0,1)
plot_state_hinton(qc)
```

B. 
```python
from qiskit import QuantumCircuit
from qiskit.visualization import plot_state_hinton

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0,1)
qc.z(1)
plot_state_hinton(qc)
```

C.
```python
from qiskit import QuantumCircuit
from qiskit.visualization import plot_state_hinton

qc = QuantumCircuit(2)
qc.h(0)
qc.x(1)
qc.cx(0,1)
plot_state_hinton(qc)
```

D.
```python
from qiskit import QuantumCircuit
from qiskit.visualization import plot_state_hinton

qc = QuantumCircuit(2)
qc.h(0)
qc.x(1)
qc.cx(0,1)
qc.z(1)
plot_state_hinton(qc)
```

6. Which **two** code fragments generate the circuit picture below?
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/Q6.png>

A. 
```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(4,2)
qc.h(0)
qc.cx(0,1)
qc.draw('mpl',idle_wires=False)
```

B. 
```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(3)
qc.h(1)
qc.cx(1,0)
qc.draw('mpl',idle_wires=False)
```

C. 
```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(4)
qc.h(1)
qc.cx(1,0)
qc.draw('mpl')
```

D. 
```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(2)
qc.h(0)
qc.cx(0,1)
qc.draw('mpl')
```

7. Which is the transpiled circuit generated by using the pass manager defined below on the displayed circuit?
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/Q7.png>

```python
pm = generate_preset_pass_manager(optimization_level=1)
qct = pm.run(qc)
qct.draw('mpl')
```

A.
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/A7a.png>

B.
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/A7b.png>

C.
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/A7c.png>

D. 
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/A7d.png>

8. Which one from the histograms below might be the ouput of this circuit?
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/Q8.png>

A. 
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/A8a.png>

B.
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/A8b.png>

C. 
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/A8c.png>

D.
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/A8d.png>

9. Which is the circuit generated using the code below?
```python
from qiskit import QuantumCircuit,QuantumRegister,ClassicalRegister
from qiskit.circuit import Parameter

theta = Parameter('theta')

qr = QuantumRegister(3,'q')
cr = ClassicalRegister(3,'c')
qc = QuantumCircuit(qr,cr)
qc.h(0)  
for i in range(1,3):
    qc.crx(i*theta,0,i)  
qc.barrier()
qc.measure(qr,cr)

qc.draw('mpl')
```

A. 
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/A9a.png>

B.
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/A9b.png>

C. 
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/A9c.png>

D. 
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/A9d.png>

10. Which **two** are proper ways to run jobs inside a session?

A. 
```python
with Session(backend=backend) as session:
    sampler1 = SamplerV2() 
    sampler2 = SamplerV2() 
    job1 = sampler1.run([(qct1,[],16)])
    job2 = sampler2.run([(qct2,[],16)])
```

B.
```python
session = Session(backend=backend)
sampler1 = SamplerV2(mode=backend) 
sampler2 = SamplerV2(mode=backend) 
job1 = sampler1.run([(qct1,[],16)])
job2 = sampler2.run([(qct2,[],16)])
```

C.
```python
session = Session(backend=backend)
sampler1 = SamplerV2() 
sampler2 = SamplerV2() 
job1 = sampler1.run([(qct1,[],16)])
job2 = sampler2.run([(qct2,[],16)])
```

D. 
```python
session = Session(backend=backend)
sampler1 = SamplerV2(mode=session) 
sampler2 = SamplerV2(mode=session) 
job1 = sampler1.run([(qct1,[],16)])
job2 = sampler2.run([(qct2,[],16)])
```

E.
```python
with Batch(backend=backend, mode="Session") as session:
    sampler1 = SamplerV2() 
    sampler2 = SamplerV2() 
    job1 = sampler1.run([(qct1,[],16)])
    job2 = sampler2.run([(qct2,[],16)])
```

11. Which is the proper way to stop a pending batch/session from accepting new jobs and preventing the pending ones from being executed.  
   
A.
```python
batch.close()  
```
B.
```python
batch.cancel() 
```
C.
```python
batch.stop()  
```
D.
```python
batch.close(complete_pending=False)  
```

12. Which **two** sentences are correct regarding compilation time in batch/session mode?  

A. In session mode, only time spent on the quantum hardware counts as usage.     
B. In batch mode, only time spent on the quantum hardware counts as usage.  
C. In batch mode, usage time includes both compilation and execution time.  
D. In session mode, usage is the wall clock time the QPU is committed to the session. That includes compilation time.  
E. Compilation time never counts as usage.  

13. When building a Sampler primitive, the format of the PUB contains ...?  

A. At most two values: a Quantum Circuit and a target precision. 
B. At most three values: a Quantum Circuit, a collection of parameters if parameters are being used and optionally a target precision.  
C. At most three values: a Quantum Circuit, a collection of parameters if parameters are being used and optionally the number of shots.   
D. At most four values: a Quantum Circuit, a list of one or more observables, a collection of parameters if Parameters are being used and optionally a target precision.  
E. At most four values: a Quantum Circuit, a list of one or more observables, a collection of parameters if Parameters are being used and optionally the number of shots.  

14. Which is the proper way to run a sampler indicating the number of shots to be 2048?

A. 
```python
from qiskit_ibm_runtime import SamplerV2 

sampler = SamplerV2(mode = backend, shots = 2048)
job = sampler.run([(qct)])
```

B. 
```python
from qiskit_ibm_runtime import SamplerV2 

sampler = SamplerV2(mode = backend)
job = sampler.run([(qct)], shots = 2048)
```

C.
```python
from qiskit_ibm_runtime import SamplerV2 

sampler = SamplerV2(mode = backend)
job = sampler.run([(qct,2048)])
```

D.
```python
from qiskit_ibm_runtime import SamplerV2 

sampler = SamplerV2(mode = backend)
job = sampler.run([(qct, shots = 2048)])
```

15. Given the circuit below, which are **two** ways to pass the parameters to the estimator?
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/Q15.png>

A. 
```python
from qiskit_ibm_runtime import EstimatorV2 
from numpy import pi

estimator = EstimatorV2(mode = backend)
parms = {theta:[0,pi/2,pi]}
job = estimator.run([(qc,observables,parms)])
```

B.  
```python
from qiskit_ibm_runtime import EstimatorV2 
from numpy import pi

estimator = EstimatorV2(mode = backend)
beta = 2*theta
parms = {beta:[0,pi/2,pi],theta:[0,pi,0,2*pi]}
job = estimator.run([(qc,observables,parms)])
```

C. 
```python
from qiskit_ibm_runtime import EstimatorV2 
from numpy import pi

estimator = EstimatorV2(mode = backend)
parms = [0,pi/2,pi]
job = estimator.run([(qct,observables,parms)])
```

D. 
```python
from qiskit_ibm_runtime import EstimatorV2 
from numpy import pi

estimator = EstimatorV2()
parms = {beta:[0,pi/2,pi]}
job = estimator.run([(qct,parms,observables)])
```

16. Which of the following statements about broadcasting rules in Qiskit Runtime primitives, as implemented by the Primitive Unified Block (PUB), is **false**?

A. Input arrays do not need to have the same number of dimensions.  
B. Dimensions with size one can be broadcast to match larger dimensions.  
C. Inputs can be combined using ZIP or outer (product) broadcasting modes.  
D. Missing dimensions are assumed to have size two. 

17. Which theta/phi assignment to the circuit below generates that histogram ?
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/Q17a.png><img src=https://github.com/gnietof/QuantumTests/blob/main/exam2/images/Q17b.png>

A. 
```python
qc.assign_parameters({'theta':pi/2,'phi': pi/2})
```

B. 
```python
qc.assign_parameters({'theta':0,'phi': 0})
```

C. 
```python
qc.assign_parameters({'theta':0,'phi': pi/2})
```

D. 
```python
qc.assign_parameters({'theta':pi,'phi': pi})
```

18. Which is the way to retrieve jobs which have been executed in a session having id SESSION?

A. 
```python
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()
session = Session.from_id(service,SESSION)
jobs = session.jobs()
```

B. 
```python
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()
session = Session.from_id(SESSION, service)
jobs = session.jobs()
```

C. 
```python
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()
jobs = service.jobs(session_id=SESSION)
```

D. 
```python
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()
jobs = service.jobs(batch_id=SESSION)
```

19. Which is the data type which is **not** available in QASM3?
   
A. Bit  
B. Angle  
C. Unsigned integer  
D. Char  
E. Complex  

20.	Which is the endpoint for cancelling a running job?

A. DELETE /v1/jobs/{id}  
B. POST /v1/jobs/{id}/stop  
C. POST /v1/jobs/{id}/cancel  
D. PUT /v1/jobs/{id}/cancel  

<details>
  <summary>Show answer key</summary>
  1. B<br>
  2. A<br>  
  3. E<br>  
  4. B<br>  
  5. B<br>
  6. AB<br>
  7. D<br>
  8. C<br>
  9. A<br>  
 10. AD<br>
 11. B<br>  
 12. BD<br>
 13. C<br>
 14. B<br>
 15. AC<br>
 16. D<br> 
 17. C<br>
 18. C<br>
 19. D<br>
 20. C<br>
</details>
