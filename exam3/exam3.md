# Qiskit 2.x Practice Exam III

1. Which **two** code fragments will generate a Pauli Y operator?

A. 
```python
from qiskit.quantum_info import Pauli
Pauli('X')@Pauli('Z')
```

B. 
```python
from qiskit.quantum_info import Pauli

Pauli([True],[True],0])
```

C. 
```python
from qiskit import QuantumCircuit
from qiskit.quantum_info import Pauli

qc = QuantumCircuit(1)
qc.y(0)
p = Pauli(qc)
```

D.
```python
from qiskit.quantum_info import Pauli
Pauli('X').tensor(Pauli('Z'))
```

2. Which **three** of these operations return the same Pauli list?

A.  
```python
SparsePauliOp.from_sparse_list([("XYZXYZ",(5,3,1,4,2,0),1)],num_qubits=6)
```

B. 
```python
Pauli('XX') @ Pauli('YY') @ Pauli('ZZ')
```

C.  
```python
Pauli('XX').tensor(Pauli('YY').tensor(Pauli('ZZ')))
```

D. 
```python
SparsePauliOp.from_sparse_list([("XXYYZZ",(0,1,2,3,4,5),1)],num_qubits=6)
```

E.  
```python
SparsePauliOp.from_list([("XXYYZZ",1)])
```

3. Given the following code fragment what is the probability that a measurement would result in a value of 1?

```python
from qiskit import QuantumCircuit
from qiskit.quantum_info import Statevector
import numpy as np

qc = QuantumCircuit(1)
qc.h(0)
qc.rz(np.pi/4,0)
```

A. 0  
B. 0.146  
C. 0.5   
D. 0.853  
E. 1  

4. Which histogram generates the following code?
```python
from qiskit.visualization import plot_histogram

plot_histogram(data,color=['blue','orange','green','red'],sort='desc')
```

A.  
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/A4a.png>

B.  
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/A4b.png>

C.  
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/A4c.png>

D.  
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/A4d.png>

5. Which is the city state diagram generated by the following code fragment? 
```python
from qiskit import QuantumCircuit
from qiskit.visualization import plot_state_city

qc = QuantumCircuit(2)
qc.h(0)
qc.x(1)
qc.cx(0,1)
```

A.
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/A5a.png>

B. 
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/A5b.png>

C. 
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/A5c.png>

D.
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/A5d.png>

6. Which **two** fragments of code generate the circuit in the image?
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/Q6.png>

A. 
```python
from qiskit import QuantumCircuit
from qiskit.circuit import Parameter

theta = Parameter('theta')
beta = Parameter('beta')
qc = QuantumCircuit(2,2)
qc.h(0)  
qc.rx(2*theta+3*beta,1)  
qc.cx(0,1)

qc.measure((0,1),(0,1))
```

B.  
```python 
from qiskit import QuantumCircuit
from qiskit.circuit import Parameter

theta = Parameter('theta')
beta = Parameter('beta')
gamma = 3*theta+2*beta
qc = QuantumCircuit(2,2)
qc.h(0)  
qc.rx(gamma,1)  
qc.cx(0,1)

qc.measure((0,1),(0,1))
```

C.  
```python
from qiskit import QuantumCircuit
from qiskit.circuit import Parameter

theta = Parameter('theta')
beta = Parameter('beta')
qc = QuantumCircuit(2,2)
qc.h(1)  
qc.rx(3*theta+2*beta,0)  

qc.measure((0,1),(0,1))
```

D.  
```python
from qiskit import QuantumCircuit
from qiskit.circuit import Parameter

theta = Parameter('theta')
beta = Parameter('beta')
qc = QuantumCircuit(2,2)
qc.h(0)  
qc.rx(3*theta+2*beta,1)  
qc.cx(0,1)

qc.measure((0,1),(0,1))
```

7. Which **two** of the following code fragments generate the circuit in the picture below?
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/Q7.png>

A. 
```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(2,2)
qc.h(0)
qc.cx(0,1)
qc.measure()
qc.draw('mpl')
```

B. 
```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(2,2)
qc.h(0)
qc.cx(0,1)
qc.measure_active(add_bits=False)
qc.draw('mpl')
```

C. 
```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(2,2)
qc.h(-2)
qc.cx(-2,-1)
qc.measure_all(add_bits=False)
qc.draw('mpl')
```

D. 
```python
from qiskit import QuantumCircuit

qc = QuantumCircuit(2,2)
qc.h(0)
qc.cx(0,1)
qc.barrier()
qc.measure((0,1),(0,1))
qc.draw('mpl')
```

8. Which is the transpiled circuit generated by using the pass manager defined below on the displayed circuit?
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/Q8.png>

```python
pm = generate_preset_pass_manager(basis_gates=['ry','rx','ecr'])
```

A. 
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/A8a.png>

B. 
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/A8b.png>

C.
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/A8c.png>

D.
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/A8d.png>

9. Which code fragment generates the circuit in the picture below?

<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/Q9.png>

A. 
```python
from qiskit import QuantumCircuit
import numpy as np

i =Parameter('i')
theta = np.pi*i/4

qc = QuantumCircuit(2,2)
with qc.for_loop(range(4)) as i:
    qc.ry(theta,0)
qc.rx(np.pi/8,1)
qc.cx(0,1)
qc.measure((0,1),(0,1))
qc.draw('mpl')
```

B. 
```python
from qiskit import QuantumCircuit
import numpy as np

i =Parameter('i')
theta = np.pi*i/8

qc = QuantumCircuit(2,2)
with qc.for_loop(range(4)) as i:
    qc.rx(theta,0)
    qc.ry(np.pi/4,1)
    qc.cx(0,1)
qc.measure((0,1),(0,1))
qc.draw('mpl')
```

C. 
```python
from qiskit import QuantumCircuit
import numpy as np

i =Parameter('i')
theta = np.pi*i/8

qc = QuantumCircuit(2,2)
with qc.for_loop(range(4)) as i:
    qc.rx(theta,0)
qc.ry(np.pi/4,1)
qc.cx(0,1)
qc.measure((0,1),(0,1))
qc.draw('mpl')
```

D. 
```python
from qiskit import QuantumCircuit
import numpy as np

i =Parameter('i')
theta = np.pi*i/8

qc = QuantumCircuit(2,2)
with qc.for_loop(range(4)) as i:
    qc.rx(theta,1)
qc.ry(np.pi/4,0)
qc.cx(0,1)
qc.measure((0,1),(0,1))
qc.draw('mpl')
```

10. How is the proper way to run the circuit below in a session using the AerSimulator?

<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/Q10.png>

A.
```python
from qiskit_aer import AerSimulator
from qiskit_ibm_runtime import Session
from qiskit_ibm_runtime import SamplerV2

sim = AerSimulator()
with Session(backend=backend) as session:
    samplers = SamplerV2(mode = sim)
    job = samplers.run([(qc)])
```

B. 
```python
from qiskit_aer import AerSimulator
from qiskit_ibm_runtime import Session
from qiskit_ibm_runtime import SamplerV2

sim = AerSimulator()
with Session(backend=sim) as session:
    samplers = SamplerV2(mode = session)
    job = samplers.run([(qc)])
```

C.
```python
from qiskit_aer import AerSimulator
from qiskit_ibm_runtime import Session
from qiskit_ibm_runtime import SamplerV2

sim = AerSimulator()
with Session(backend=sim) as session:
    samplers = SamplerV2(mode = session)
    job = samplers.run([(qc,params)])
```

D. It is not possible to run a Session on the AerSimulator.

11. Which is the proper way to retrieve information about one bactch with the id ID created in a QPU named BACKEND?

A.
```python
from qiskit_ibm_runtime import QiskitRuntimeService
service = QiskitRuntimeService()
backend = service.backend(BACKEND)
bactch = backend.bactch(ID)
```

B.
```python
from qiskit_ibm_runtime import QiskitRuntimeService
service = QiskitRuntimeService()
backend = service.backend(BACKEND)
bactch = backend.job(ID)
```

C.
```python
from qiskit_ibm_runtime import QiskitRuntimeService
service = QiskitRuntimeService()
backend = service.backend(BACKEND)
bactch = backend.job(ID,mode="Batch")
```

D. 
```python
from qiskit_ibm_runtime import QiskitRuntimeService
service = QiskitRuntimeService()
bactch = Batch.from_id(ID,service)
```

12. How many jobs can be submitted as part of a a batch?

A. There is no limit to the number of jobs.  
B. There is no limit to the number of jobs, but there is a maximum time after which no more jobs will be accepted.  
C. The number of jobs depends on the complexity of the circuits (its depth and the number of qubits being used in each submitted circuit.  
D. The maximum number of jobs is limited to 10 by the system.  

13. Which is the proper way to retrieve the sampler results from the circuit shown below?

<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/Q13.png>

A. 
```python
from qiskit_ibm_runtime import SamplerV2

sampler = SamplerV2(mode = backend)
job = sampler.run([(qct)])
job.result()[0].data.meas.get_counts()
```

B. 
```python
from qiskit_ibm_runtime import SamplerV2

sampler = SamplerV2(mode = backend)
job = sampler.run([(qct)])
job.result()[0].data.c.counts()
```

C. 
```python
from qiskit_ibm_runtime import SamplerV2

sampler = SamplerV2(mode = backend)
job = sampler.run([(qct)])
job.result()[0].get_counts()
```

D. 
```python
from qiskit_ibm_runtime import SamplerV2

sampler = SamplerV2(mode = backend)
job = sampler.run([(qct)])
job.result()[0].data.c.get_counts()
```

14. Given the circuit below, which are **two** ways to pass the parameters to the sampler?
```python
from qiskit import QuantumCircuit
from qiskit.circuit.library import RYGate
from qiskit.circuit import Parameter

theta = Parameter('theta')
beta = 2*theta
qc = QuantumCircuit(2,2)
qc.rx(theta,0)  
ry = RYGate(beta).control(1)
qc.append(ry,[0,1])
qc.measure((0,1),(0,1))
```
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/Q14.png>

A.  
```python
from qiskit_ibm_runtime import SamplerV2 
from numpy import pi

sampler = SamplerV2(mode = backend)
parms = {theta:[0,pi/2,pi]}
job = sampler.run([(qc,parms)])
```

B.  
```python
from qiskit_ibm_runtime import SamplerV2 
from numpy import pi

sampler = SamplerV2(mode = backend)
parms = {beta:[0,pi/2,pi],theta:[0,pi,0,2*pi]}
job = sampler.run([(qc,parms)])
```

C. 
```python
from qiskit_ibm_runtime import SamplerV2 
from numpy import pi

sampler = SamplerV2(mode = backend)
parms = [0,pi/2,pi]
job = sampler.run([(qct,parms)])
```

D. 
```python
from qiskit_ibm_runtime import SamplerV2 
from numpy import pi

sampler = SamplerV2(mode = backend)
parms = {beta:[0,pi/2,pi],theta:None}
job = sampler.run([(qct,parms)])
```

15. Which are the right steps to run an estimator?  

A. 
- Circuit construction
- Observable definition
- Transpilation 
- Observable transformation (applying the layout)
- Primitive(s) construction
- Service initialization & backend retrieval
- Execution and Retrieval

B. 
- Service initialization & backend retrieval
- Circuit construction
- Observable definition
- Transpilation 
- Observable transformation (applying the layout)
- Primitive(s) construction
- Execution and retrieval

C. 
- Service initialization & backend retrieval
- Circuit construction
- Observable definition
- Observable transformation (applying the layout)
- Transpilation 
- Primitive(s) construction
- Execution and retrieval

D. 
- Service initialization & backend retrieval
- Circuit construction
- Primitive(s) construction
- Observable definition
- Observable transformation (applying the layout)
- Transpilation 
- Execution and retrieval

16. Which is the proper way to run an estimator indicating the required precision?

A. 
```python
from qiskit_ibm_runtime import EstimatorV2 

estimator = EstimatorV2(mode = backend)
job = estimator.run([(qct, observable)],precision=0.0078125)])
```

B.
```python
from qiskit_ibm_runtime import EstimatorV2 

estimator = EstimatorV2(mode = backend)
job = estimator.run([(qct, observable,precision=0.0078125)])
```

C.
```python
from qiskit_ibm_runtime import EstimatorV2 

estimator = EstimatorV2(mode = backend,precision=0.0078125)
job = estimator.run([(qct, observable)])
```

D.
```python
from qiskit_ibm_runtime import EstimatorV2 

estimator = EstimatorV2(mode = backend)
job = estimator.run([(qct, observable)],0.0078125)
```

17. Which is the expected output for this circuit?
```python
qc = QuantumCircuit(2,2)
qc.h(1)
qc.cx(0,1)
qc.h(1)
```

A. $\ket{00}$  
B. $\ket{01}$  
C. $\ket{10}$  
D. $\ket{11}$  

18. Which is the way to retrieve jobs which have been executed in backend named BACKEND?

A. 
```python
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()
backend = service.backend(BACKEND)
jobs = backend.jobs()
```

B. 
```python
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()
jobs = service.jobs(backend_name=BACKEND)
```

C. 
```python
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()
backend = service.backend(BACKEND)
jobs = service.jobs(backend=backend)
```

D. 
```python
from qiskit_ibm_runtime import QiskitRuntimeService

service = QiskitRuntimeService()
jobs = service.jobs(BACKEND)
```

19. How are the jobs for a single backend retrieved?

A. GET /v1/jobs/backends/{id}  
B. GET /jobs?backend={id}  
C. POST /v1/backends/{id}/jobs  
D. GET /backend?jobs={id}  

20. Which is the QASM3 which generates the circuit in the picture?
<img src=https://github.com/gnietof/QuantumTests/blob/main/exam3/images/Q20.png>

A.  
```qasm
OPENQASM 3.0;
include "stdgates.inc";
bit[2] c;
qubit[2] q;
h q[0];
rx pi/4, q[1];
cx q[0], q[1];
barrier q[0], q[1];
c[0] = measure q[0];
c[1] = measure q[1];
```

B. 
```qasm
OPENQASM 3.0;
include "stdgates.inc";
bit[2] c;
qubit[2] q;
h q[0];
rx(pi/4) q[1];
cx q[0], q[1];
barrier q[0], q[1];
c[0] = measure q[0];
c[1] = measure q[1];
```

C. 
```qasm
OPENQASM 3.0;
include "stdgates.inc";
bit[2] c;
qubit[2] q;
h q[0];
rx(pi/4) q[1];
cx q[0] q[1];
barrier q[0] q[1];
c[0] = measure q[0];
c[1] = measure q[1];
```

D. 
```qasm
OPENQASM 3.0;
include "stdgates.inc";
bit[2] c;
qubit[2] q;
h q[0];
rx pi/4 q[1];
cx q[1] q[0];
barrier q[0] q[1];
c[0] = measure q[0];
c[1] = measure q[1];
```

<details>
  <summary>Show answer key</summary>
  1. BC<br>
  2. ACE<br>  
  3. C<br>  
  4. B<br>  
  5. C<br>
  6. BD<br>
  7. CD<br>
  8. A<br>
  9. C<br>  
 10. B<br>
 11. D<br>  
 12. B<br>
 13. D<br>
 14. AC<br>
 15. B<br>
 16. A<br> 
 17. A<br>
 18. B<br>
 19. B<br>
 20. B<br>
</details>
